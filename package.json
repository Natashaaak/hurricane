{
  "name": "hurricane-visualization",
  "version": "1.0.0",
  "description": "Hurricane Isabel 3D Visualization",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "three": "^0.175.0",
    "vite": "^6.2.6"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
